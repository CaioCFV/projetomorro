import styled from "styled-components";

export const Main = styled.table`
  width: 100%;

  th {
    border-bottom: 1px solid #ddd;
    td {
      font-weight: 700;
    }
  }
  td {
    padding: 10px;
    color: var(--color-4);
    font-size: 13px;
  }
`;
